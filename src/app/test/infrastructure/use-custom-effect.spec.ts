/**
 * @jest-environment jsdom
 */
import { renderHook, act } from "@testing-library/react";
describe("useCustomEffect", () => {
  test("should called once on develop env", () => {});
  test("should used eruda on telegram", () => {});
});
